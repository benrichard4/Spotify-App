import React from "react";
import { StyledHeader, Title } from "./styled-components";

const Header = () => {
  return (
    <StyledHeader>
      <Title>Spotify Artist Search</Title>
    </StyledHeader>
  );
};

export default Header;
